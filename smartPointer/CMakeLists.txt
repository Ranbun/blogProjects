set(project_name smartpointer)
project(${project_name})

set(headers 
    
)

set(sources_file
    main.cpp
)


add_executable(${project_name} 
               ${sources_file}
#               ${headers}
)
